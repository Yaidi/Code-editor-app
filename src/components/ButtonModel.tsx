import React, { ReactElement } from 'react';
import Button from '@material-ui/core/Button';

const ButtonGeneric = (): ReactElement => {
  return <Button variant="contained">Default</Button>;
};
export default ButtonGeneric;
